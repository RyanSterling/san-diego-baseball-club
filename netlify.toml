[build]
  command = "npm run build"
  publish = ".next"

[functions]
  # Increase function timeout for AI extraction (requires Pro plan for 60s)
  node_bundler = "esbuild"

[[plugins]]
  package = "@netlify/plugin-nextjs"
